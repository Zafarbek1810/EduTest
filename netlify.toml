[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# React Router (SPA) — barcha yo'llar index.html ga
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
